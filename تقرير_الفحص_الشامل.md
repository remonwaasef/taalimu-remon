# تقرير الفحص الشامل للمشروع

**المشروع:** منصة تعليمية متعددة المستأجرين (Multi-Tenant LMS) — Laravel 12 / PHP 8.2+
**البنية:** تطبيق Laravel مقسّم إلى وحدات (Modules: Center, Instructor, Campus, Admin, Api, Tenancy) عبر `nwidart/laravel-modules`
**تاريخ الفحص:** 5 يوليو 2026
**نطاق الفحص:** الأمان، الأداء وعنق الزجاجة، جودة الكود، الهيكلة، ومبدأ DRY

---

## 1. الملخّص التنفيذي

المشروع **ناضج ومبنيّ بوعي أمني عالٍ** مقارنةً بالمعتاد. هناك طبقات حماية واضحة: عزل مستأجرين تلقائي (`TenantScope`)، سياسات صلاحيات (Policies)، تحقّق HMAC لبوابات الدفع، WAF بسيط، CSP، ورؤوس أمان. معظم الكونترولرز تستخدم `authorize()` وتقييد `tenant_id` معًا (دفاع في العمق).

لكن يوجد عدد محدود من الثغرات الحقيقية التي تستحق الإصلاح الفوري — أبرزها **كشف بيانات عبر مسار عام غير محمي (IDOR)** و**كلمات مرور افتراضية ضعيفة وقابلة للتخمين**.

**التقييم العام:**

| المحور | التقدير |
|--------|---------|
| الأمان | جيد جدًا مع ثغرات محدّدة يجب معالجتها |
| الأداء | جيد جدًا (كاش + فهارس + تجميع في قاعدة البيانات) |
| جودة الكود والهيكلة | جيد (فصل مسؤوليات واضح: Services / Queries / DTOs / Policies) |
| DRY | جيد مع تكرارات بسيطة قابلة للتوحيد |

---

## 2. الأمان

### 2.1 حرجة / عالية الأولوية

**(H-1) كشف بيانات عبر مسار عام غير محمي — IDOR**
الموقع: `Modules/Center/routes/web.php:64` + `StudentRegistrationController::success()` (السطر 109).

```php
Route::get('register/success/{user}', [...'success'])->name('group.registration.success');
```

المسار **عام تمامًا** (معرّف قبل مجموعة `auth`) ويستقبل `{user}` كـ ID رقمي متسلسل. الدالة `success()` تعرض للصفحة: اسم الطالب، والـ `qr_identifier` (وهو نفسه المُستخدم لتسجيل الحضور!)، بالإضافة إلى رابط بوابة موقّع صالح 30 يومًا (`temporarySignedRoute('student.portal', now()->addDays(30), ...)`).

الأثر: يمكن لأي شخص عبر تعداد `/register/success/1,2,3...` الحصول على أسماء الطلاب ومعرّفات QR الخاصة بهم (تزوير الحضور) ورابط دخول بوابة صالح لمدة شهر لأي طالب.

المعالجة المقترحة: توقيع المسار (`signed`) أو ربطه بالجلسة (عرض بيانات المستخدم المُنشأ للتو فقط عبر session flash) بدل تمرير الـ ID في الرابط.

---

**(H-2) كلمات مرور افتراضية ضعيفة وقابلة للتخمين**

1. التسجيل الذاتي عبر التوكن — `StudentRegistrationController.php:69`:
```php
'password' => Hash::make($request->phone), // Default password is phone
```
كلمة المرور = رقم الهاتف نفسه، **ولا يُضبط علم `must_change_password`** عند الإنشاء هنا (بخلاف الاستيراد). فمن يعرف رقم هاتف الطالب يستطيع تسجيل الدخول.

2. الاستيراد الجماعي — كلمة مرور موحّدة `password123` معروضة صراحةً في الواجهة (`students/import.blade.php:157`).

المعالجة: توليد كلمة مرور عشوائية + إجبار التغيير عند أول دخول (`must_change_password = true`) في كل مسارات إنشاء الحسابات، بما فيها التسجيل الذاتي.

---

**(H-3) غياب Rate-limiting و CAPTCHA على التسجيل العام**
المسار `POST register/group/{token}` (السطر 63) لا يحمل أي `throttle`. يسمح بإنشاء حسابات وفواتير بالجملة (Sale تُنشأ لكل تسجيل عبر `financeService->createSale`)، ما يفتح باب إغراق قاعدة البيانات (Resource Exhaustion / Spam). أضف `throttle` و CAPTCHA.

### 2.2 متوسطة

**(M-1) تسريب معرّف QR إلى طرف ثالث**
في عدة أماكن يُرسَل `qr_identifier` (سر الحضور) داخل رابط لخدمة خارجية:
```
https://api.qrserver.com/v1/create-qr-code/?...&data=urlencode($user->qr_identifier)
```
(`StudentRegistrationController.php:116`، وعدة views). المعرّف السري يُسرَّب لخدمة خارجية عبر HTTP GET. يُفضّل توليد الـ QR محليًا (المكتبة `bacon/bacon-qr-code` و`chillerlan/php-qrcode` **مثبّتة أصلًا** في المشروع).

**(M-2) `qr_identifier` بطول 12 حرفًا فقط عبر `Str::random`**
يُستخدم كمعرّف مصادقة للحضور والبوابة. مقبول لكن يُفضّل رفعه إلى 32+ حرفًا أو استخدام `Str::uuid()`، خصوصًا مع تسريبه لطرف ثالث (M-1).

**(M-3) CSP يسمح بـ `unsafe-inline` للسكربتات في الإنتاج**
`ContentSecurityPolicy.php:41` — `script-src 'self' 'unsafe-inline' ...`. هذا يُضعف الحماية ضد XSS. التعليق في الكود يعترف بذلك ويعِد بخطة nonce لاحقة. توصية: تفعيل خطة الـ nonce/Vite لإزالة `unsafe-inline`.

**(M-4) الاعتماد على قيم البيئة في `is_relaxed_throttle_env()`**
عند التطوير يُرفع الحد إلى 100 محاولة. تأكّد أن الشرط مبني على البيئة فقط (وهو كذلك حاليًا — تعليقات الكود صريحة أن IP لا يُخفّف الحد)، لكن راقب ألا يتسرّب `relaxed` إلى الإنتاج عبر أي متغيّر بيئة.

### 2.3 نقاط أمان إيجابية (موثّقة)

- **حقن SQL:** لم أعثر على تمرير مدخلات مستخدم مباشرة داخل `whereRaw/DB::raw`. كل استخدامات `Raw` تحوي ثوابت أو أعمدة داخلية فقط، وفلترة `LIKE` تُهرَّب عبر `QueryHelper::escapeLike` (`StudentQuery.php:15`).
- **Mass Assignment:** لا يوجد أي `$guarded = []` ولا `create($request->all())`؛ تُستخدم Form Requests و`$request->validated()`.
- **رفع الملفات:** `HandlesFileUploads` محكم — قائمة MIME بيضاء، مطابقة الامتداد مع الـ MIME، حظر الامتدادات المزدوجة الخطرة، اسم عشوائي، عزل حسب المستأجر، وإزالة SVG لتفادي XSS.
- **XSS في القوالب:** كل استخدامات `{!! !!}` آمنة (إمّا `nl2br(e(...))`، أو `json_encode`، أو محتوى منقّى عبر `sanitizedContent()`/HTMLPurifier، أو ترجمات ثابتة).
- **عزل المستأجرين:** `TenantScope` عام + تقييد صريح بـ `tenant_id` في الكونترولرز = دفاع في العمق. استخدامات `withoutGlobalScope` محدودة ومبرّرة.
- **الدفع:** تحقّق HMAC عبر `hash_hmac('sha512')` + `hash_equals` (مقارنة زمن ثابت)، واستثناء webhooks من CSRF مع تحقّق التوقيع داخليًا.
- **المصادقة:** Rate-limiting على تسجيل الدخول (IP+email)، `session()->regenerate()`، `SESSION_SECURE_COOKIE=true`، `APP_DEBUG=false`، `APP_ENV=production`.
- **IDOR في المسارات المحمية:** ممتاز — كل الوصول عبر ID مقيّد بـ `where('tenant_id', ...)->findOrFail()` + `authorize()`.

---

## 3. الأداء وعنق الزجاجة

### 3.1 نقاط قوة

- **كاش على مستوى المستأجر:** `TenantCache` يضيف بادئة `tenant_{id}_` لكل مفتاح، ويُستخدم في لوحة القيادة (15د) والتحليلات (5د) والرؤى (30د).
- **تجميع في قاعدة البيانات بدل PHP:** أكبر المتصدّرين والمدينين والإحصاءات تُحسب عبر `withSum/withCount/selectRaw` لا عبر تحميل مجموعات كبيرة.
- **فهارس أداء:** توجد 3 هجرات فهرسة مخصّصة (`add_performance_indexes`, `add_critical_performance_indexes`) تغطي `tenant_id + paid_at/date/status`، ملفوفة بـ try/catch للأمان عند إعادة التشغيل.
- **حدود Sargable:** لوحة القيادة تستخدم `whereBetween('created_at', [startOfMonth, endOfMonth])` بدل `MONTH()/YEAR()` لتفادي مسح الجدول الكامل (معلّق في الكود صراحةً).
- **Pagination:** القوائم الثقيلة مقسّمة (`paginate(10/15)`)، وبحث الطلاب يُرجع 20 صفًا كحد أقصى بدل تحميل القائمة كاملة.

### 3.2 ملاحظات للتحسين

**(P-1) N+1 محتمل في قائمة الطلاب**
`StudentController::index` (السطر 95–107) يحمّل `enrollments.course` و`sales` ثم يجمع يدويًا. الـ eager loading موجود (جيد)، لكن الحساب المالي يتكرّر لكل صف؛ إن كبر عدد الحجوزات لكل طالب يُفضّل نقله إلى `withSum`.

**(P-2) استعلامات لوحة القيادة رغم الكاش**
`getAtRiskStudents` يستخدم `ROW_NUMBER() OVER(...)` على `quiz_attempts` كاملة للمستأجر. مع نمو البيانات قد يثقُل؛ الكاش (30د) يخفّف الأثر لكن راقب زمن أول طلب بعد انتهاء الكاش (cold cache) على المستأجرين الكبار.

**(P-3) إبطال الكاش يدوي ومحدود**
مفاتيح مثل `dashboard_stats_v3` تُمسح فقط عند بذر/حذف البيانات التجريبية، لا عند كل عملية بيع/حضور. مقبول كتصميم (بيانات قد تتأخّر حتى 15د)، لكن وضّحه كسلوك متوقّع أو أبطِل الكاش عند الأحداث المالية المهمة.

**(P-4) استعلام في حلقة داخل View**
`leaderboard/index.blade.php:116` ينفّذ `PointLog::with('user')->latest()->take(5)->get()` داخل القالب. يُفضّل نقله إلى الكونترولر.

---

## 4. جودة الكود والهيكلة

### 4.1 نقاط قوة

- **فصل مسؤوليات واضح:** طبقات `Services/`, `Queries/`, `DTOs/`, `Policies/`, `Jobs/`, `Observers/`, `Notifications/` — بنية احترافية.
- **Form Requests** للتحقق (`StoreStudentRequest`, `UpdateQuizRequest`...).
- **Traits مشتركة** مدروسة (`HandlesFileUploads`, `BelongsToTenant`, `HasRoleCheck`).
- **معالجة استثناءات مركزية** في `bootstrap/app.php` مع تمييز الأخطاء المتوقّعة عن الحرجة، وإخفاء التفاصيل في الإنتاج.
- **تعليقات توضيحية جيدة** (بالعربية والإنجليزية) تشرح قرارات الأمان والأداء.

### 4.2 ملاحظات

**(Q-1) كونترولرز ضخمة**
`StudentController` (464 سطرًا)، `InstructorController` بالوحدتين (~300)، `SaleController` (359)، `AttendanceController` (392)، `AnalyticsController` (329). تتجاوز الحجم الصحي للكونترولر. يُنصح بنقل منطق الأعمال إلى Services/Actions إضافية.

**(Q-2) كود ميّت / Placeholder**
`CenterController` يحوي دوال فارغة من سقّالة الوحدة: `store(){}`, `update(){}`, `destroy(){}`, و`show/create/edit` تُرجع views فقط (السطور 264–302). احذفها إن لم تُستخدم.

**(Q-3) مسار "إصلاح لمرة واحدة" في الإنتاج**
`onboarding/fix-invoices` (تعليق: "زُر هذا الرابط مرة واحدة")، ومسار `demo/seed`. مقبولة لأنها محميّة بالصلاحيات، لكنها ديون فنية يُفضّل نقلها إلى Artisan Commands.

**(Q-4) نصوص مكتوبة صلبة (Hardcoded)**
رسائل عربية مباشرة في الكود بدل ملفات الترجمة، مثل `DemoDataController` (`'تمت إضافة البيانات...'`) و`OnboardingController`. المشروع يدعم i18n (ar/en/fr) فاستخدم `__()` باطّراد.

**(Q-5) `\App\Models\...` و `\DB::` بمسارات كاملة مضمّنة**
كثرة الإشارات المؤهّلة بالكامل داخل الجسم بدل `use` في الأعلى تقلّل القراءة (مثلًا `CenterController`, `StudentController`).

---

## 5. مبدأ DRY (عدم التكرار)

عمومًا التكرار **منخفض** بفضل الـ Traits والـ Services، مع بؤر بسيطة:

**(D-1)** تكرار جلب قائمة الدورات:
```php
\App\Models\Course::where('tenant_id', $this->tenant->id)->orderBy('title')->get();
```
مكرّر في `StudentController` (السطر 110 و123) وغيره. مرشّح لـ scope مثل `Course::forTenantOrdered()` (لاحظ أن `TenantScope` يجعل شرط `tenant_id` زائدًا أصلًا في معظم الحالات).

**(D-2)** نمط `Sale::where('tenant_id', $tenant->id)->findOrFail($id)` مكرّر ~9 مرات في `SaleController`/`UserController`. مرشّح لدالة مساعدة مثل `findSaleOrFail($id)` (كما فُعِل بالفعل بأناقة في `StudentController::findStudentOrFail`).

**(D-3)** كتلة `TenantCache::forget('dashboard_stats_v3'); TenantCache::forget('recent_activities');` مكرّرة في `DemoDataController` (seed/destroy). يوجد Trait `ClearsDashboardCache` — استخدمه هنا لتوحيدها.

**(D-4)** رؤوس الأمان مكرّرة جزئيًا بين `SecurityHeaders` و`ContentSecurityPolicy` (كلاهما يضبط `X-Content-Type-Options`, `X-Frame-Options`, `X-XSS-Protection`, `Referrer-Policy`, `Permissions-Policy`). دمجهما في middleware واحد يمنع التعارض (لاحظ اختلافهما: أحدهما `SAMEORIGIN` والآخر `DENY` لـ X-Frame-Options).

---

## 6. جدول أولويات المعالجة

| # | البند | المحور | الأولوية |
|---|------|--------|----------|
| H-1 | IDOR في `register/success/{user}` — كشف QR ورابط بوابة | أمان | 🔴 فورية |
| H-2 | كلمات مرور افتراضية = الهاتف / `password123` بلا إجبار تغيير | أمان | 🔴 فورية |
| H-3 | لا Rate-limit/CAPTCHA على التسجيل العام | أمان | 🟠 عالية |
| M-1 | تسريب `qr_identifier` لخدمة QR خارجية | أمان | 🟠 عالية |
| M-3 | CSP يسمح `unsafe-inline` للسكربتات | أمان | 🟡 متوسطة |
| M-2 | طول `qr_identifier` 12 حرفًا | أمان | 🟡 متوسطة |
| P-2/P-3 | كاش لوحة القيادة البارد + إبطال يدوي | أداء | 🟡 متوسطة |
| Q-1 | كونترولرز ضخمة → Services | جودة | 🟡 متوسطة |
| D-1..D-4 | توحيد التكرارات | DRY | 🟢 منخفضة |
| Q-2/Q-4 | كود ميّت + نصوص صلبة | جودة | 🟢 منخفضة |

---

## 7. الخلاصة

المشروع مبنيّ على أساس هندسي وأمني **قوي**: عزل مستأجرين محكم، سياسات صلاحيات شاملة، معالجة رفع ملفات نموذجية، وتحسينات أداء مدروسة (كاش + فهارس + تجميع). أبرز ما يجب معالجته فورًا هو **مسار النجاح العام (H-1)** و**كلمات المرور الافتراضية (H-2)**؛ معالجتهما ترفع الوضع الأمني إلى مستوى ممتاز. أمّا ملاحظات الأداء والجودة و DRY فهي تحسينات تدريجية لا تعوّق التشغيل.

*ملاحظة منهجية: هذا الفحص ثابت (Static) قائم على قراءة الكود. يُنصح بإكماله بـ: تشغيل `composer audit` لفحص ثغرات الحزم، وأداة SAST (مثل Larastan/PHPStan)، واختبار اختراق ديناميكي على بيئة staging.*

---

## 8. الإصلاحات المطبّقة (تحديث)

| البند | ما تم تنفيذه | الملفات |
|------|--------------|---------|
| **H-1** IDOR | ربط صفحة النجاح بجلسة المُسجِّل عبر `session('registered_user_id')` + `abort_unless(...403)`، فلا يمكن تعداد `/register/success/{id}` لسحب بيانات طلاب آخرين | `StudentRegistrationController.php` |
| **H-2** كلمات المرور | ضبط `must_change_password = true` عند التسجيل الذاتي لإجبار تغيير كلمة المرور عند أول دخول (لم تعد الهاتف صالحة بشكل دائم)، ورفع طول `qr_identifier` إلى 32 حرفًا | `StudentRegistrationController.php` |
| **H-3** Rate-limit | إضافة `throttle:registration` على `POST` التسجيل و`throttle:30,1` على صفحتي العرض والنجاح | `Modules/Center/routes/web.php` |
| **M-1** تسريب QR | استبدال كل استدعاءات `api.qrserver.com` بتوليد QR **محليًا في المتصفح** (مكتبة qrcodejs)، بما يشمل معرّفات QR وروابط الدخول السحري الموقّعة و`student_id` | `StudentRegistrationController.php` + 5 قوالب Blade (Center/Instructor: بطاقة الطالب، تذكرة كلمة المرور، قوائم الطلاب) |

**ملاحظات ما بعد الإصلاح:**

- الرسالة في صفحة النجاح ("ادخل بهاتفك ككلمة سر افتراضية") ما زالت صحيحة لأن الهاتف يبقى كلمة مرور *مؤقتة* تُجبَر على التغيير فورًا.
- توليد QR أصبح محليًا (client-side) على كل الصفحات المعنية؛ لا تُرسَل أي بيانات حساسة لأي خدمة خارجية. مكتبة `qrcodejs` تُحمَّل من `cdnjs` المسموح به أصلًا في سياسة CSP.
- **لم يُنفَّذ بعد (يُترك لقرارك):** M-2 (زيادة الأعمدة كانت جزئية)، M-3 (إزالة `unsafe-inline` من CSP — تحتاج خطة nonce/Vite)، وبنود الجودة/DRY (Q/D). هذه تحسينات غير عاجلة.
- **تحقق مطلوب قبل النشر:** شغّل `php artisan config:clear && php artisan view:clear`، ثم اختبر مسار التسجيل الذاتي وعرض بطاقات QR على بيئة staging (تعذّر تشغيل linter آليًا هنا بسبب قيود البيئة، لذا روجعت التعديلات يدويًا).
