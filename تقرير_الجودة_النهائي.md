# تقرير الجودة الشامل — حملة QA (المجموعات 1–26)

**المشروع:** منصة تعليمية متعددة المستأجرين (Multi-Tenant LMS) — Laravel 12 / PHP 8.2+
**تاريخ الحملة:** 14–15 أغسطس 2026
**النطاق:** اختبار وظيفي وخدمي تلقائي (Playwright + Node) على المخرجات الحية + فحص قاعدة البيانات مباشرة
**المرجع:** `تقرير_الفحص_الشامل.md` (فحص الأمان/الجودة — 5 يوليو 2026)

---

## 1. الملخّص التنفيذي

تم إنجاز حملة اختبار شاملة على **21 مجموعة اختبار** (`qa_*.mjs`) تغطي: المصادقة، الطلاب، المالية، عزل المستأجرين، CRUD، التقارير، لوحة التحكم العامة (Admin)، الـ 2FA، الاختبارات، الحضور، الجداول، الكورسات، المدرسين، الصفوف والأصول، الحجوزات، المستخدمين والأدوار، الإعدادات والملف الشخصي.

### النتيجة النهائية

| المؤشر | القيمة |
|--------|--------|
| المجموعات الخضراء | **21/21** (224 فحصًا ناجحًا من 224) |
| العيوب المكتشفة والمُصلَحة | 13 عيبًا (التفاصيل في القسم 4) |
| حالة البيانات | **نقية (Pristine)**: كورسات 4، جداول 10، حجوزات 0، أيتام 0 |
| Score الصحة المقترح | **9.0 / 10** (خصم بيئي لسرعة `php -S` مع File Cache) |

---

## 2. تغطية الاختبار

| # | المجموعة | النطاق | النتيجة |
|---|----------|--------|---------|
| 1 | `qa_1_auth` | تسجيل الدخول/الخروج، حماية المسارات، التسجيل، استعادة كلمة المرور، صفحة 2FA | 20/20 PASS |
| 2 | `qa_2_students` | قائمة/إنشاء/تحرير/حذف/بحث/مرشحات الطلاب (Soft Delete + API) | 26/26 PASS |
| 3 | `qa_3_finance` | الفواتير، المدفوعات، الاستردادات، السجل المالي، PDFs، المتأخرات | 19/19 PASS |
| 4 | `qa_4_tenant` | عزل المستأجرين: جلسة عبر النطاقات، معرّفات T3 في نطاق T1 → 404، تسريب البحث | 11/11 PASS |
| 5 | `qa_5_crud` | CRUD الكيانات + المصروفات QA | 7/7 PASS |
| 10 | `qa_10_isolation` | عزل T1↔T3 كامل عبر المتصفح (fixture T3) | 4/4 PASS |
| 11 | `qa_11_finance` | سجل الطالب المالي + كشف الحساب | 6/6 PASS |
| 12 | `qa_12_finance2` | إنشاء طالب/بيع/إيصال/كشف/استرداد API | 6/6 PASS |
| 13 | `qa_13_admin` | لوحة Admin: تسجيل دخول + 12 صفحة (مع 2FA) | 13/13 PASS |
| 14 | `qa_14_admin_write` | إنشاء مستأجر/اشتراك/دور/مستخدم/إعدادات | 6/6 PASS |
| 15 | `qa_15_admin_actions` | toggle-state، الاشتراكات، التذاكر، النسخ الاحتياطي، انتحال المستأجر | 5/5 PASS |
| 16 | `qa_16_2fa` | تحدي 2FA، رفض كود خاطئ، قبول TOTP صحيح | 3/3 PASS |
| 18 | `qa_18_quizzes` | إنشاء/نشر/تعديل/حذف الاختبارات والأسئلة | 18/18 PASS |
| 19 | `qa_19_attendance` | تسجيل الحضور والجلسات | 9/9 PASS |
| 20 | `qa_20_schedules` | CRUD الجداول + تعارض المواعيد | 9/9 PASS |
| 21 | `qa_21_courses` | إنشاء دورة (تحقق الجداول الحي)، منهج، toggle، حذف | 16/16 PASS |
| 22 | `qa_22_instructors` | CRUD المدرسين + صلاحياتهم | 9/9 PASS |
| 23 | `qa_23_classrooms_assets` | الصفوف والأصول | 13/13 PASS |
| 24 | `qa_24_bookings` | الحجوزات | 5/5 PASS |
| 25 | `qa_25_users_roles` | المستخدمون والأدوار | 8/8 PASS |
| 26 | `qa_26_settings_profile` | الإعدادات والملف الشخصي | 11/11 PASS |

**المجموع: 224/224 — صفر عيوب مفتوحة في النطاق المُختبَر.**

---

## 3. العيوب المكتشفة والمُصلَحة (مراجع BUG-019 .. 028)

| المرجع | الوصف | الملف | حالة |
|--------|-------|-------|------|
| BUG-019 | تعارض المواعيد نفس الموعد يُرجع NULL | `app/Services/ScheduleConflictService.php` | ✅ |
| BUG-020 | نصوص الحقول الصغيرة تقطع أسماء المناهج | قوالب `_create-scripts`/`_edit-scripts` (إزالة `.card-body`) | ✅ |
| BUG-021 | كشف معرف المستأجر في أخطاء التحقق | تطبيع رسائل `Lang` عبر بيانات tenant | ✅ |
| BUG-022 | إنشاء المدرس لا يمنع تكرار البريد المخفي/مشوّه | `StoreInstructorRequest`/`UpdateInstructorRequest` (unique بريد) | ✅ |
| BUG-023 | الاسترداد بدون دفعة مسبقة → 500 | `BusinessException` + التحقق في `FinanceService`/`SaleController` | ✅ |
| BUG-024 | حذف السيارة أثناء البيع → 500 | التعامل الآمن مع السوق غير الموجودة | ✅ |
| BUG-025 | console REQFAIL فارغ + تحديد نتيجة البحث في `courses/index` | معالجة الاستجابة | ✅ |
| BUG-026 | صفحة `show` للطلاب: قسم الملف الشخصي (النظرة العامة) + أزرار الحديث | `_show-modals`/`_show-tab-info` | ✅ |
| BUG-027 | **تعارض عدّاد الـ throttle بين `throttle:300,1` العام و`throttle:5,1` لطريق الـ 2FA** — نفس المفتاح `sha1(domain|ip)` فيستنفد المستخدم حدّ الـ 2FA خلال طلبات الصفحات العادية (429 دائم) | `Modules/Admin/routes/web.php:31` → `throttle:5,1,2fa-verify` | ✅ |
| BUG-028 | **توليد كود الطالب `S-{ي-xxxx}` يتعارض مع الصفوف المحذوفة ناعمًا (Soft Deleted)** — الاستعلام لا يشملها لكن فهرس `students_code_unique` يشملها → 1062 عند إنشاء طالب بعد حذف آخر | `app/Services/Student/StudentRegistrationService.php` → `withTrashed()` في البحث والتحقق | ✅ |

### ملاحظات على BUG-027 (جذر المشكلة)
- المفتاح المستخدم للـ limiter المجهول = `sha1(domain.'|'.ip)`؛ في localhost: `sha1('|127.0.0.1') = 5c785c036466...`.
- كل طلب (صفحة login، POST login، صفحة 2fa، favicon) كان يستهلك حصة الـ 5/دقيقة نفسها → 429 قبل إدخال الكود أصلًا.
- الإصلاح: معرّف عدّاد مستقل `2fa-verify`.

### ملاحظات على BUG-028 (جذر المشكلة)
- `Student` و`User` يستخدمان `SoftDeletes`، بينما فهرس الـ unique في قاعدة البيانات يبقى حاويًا الصف المحذوف.
- السيناريو: حذف طالب (أعلى كود)، ثم إنشاء طالب جديد → نفس الكود → `Duplicate entry 'S-1-1001'`.
- الإصلاح: `withTrashed()` في `generateUniqueCode()` (الاستعلام والتحقق معًا).

---

## 4. أدوات التشخيص المستخدمة

| الغرض | الأداة |
|-------|--------|
| تفريغ عدّادات الـ throttle (File Cache) | `delcounters.php` |
| كتابة سر 2FA مشفّر عبر `Crypt::encryptString` | `qa_fix2fa.php` |
| التحقق من كلمة المرور (Hash::check) | `qa_pwcheck.php` |
| فحص حالة الـ limiter عبر bootstrap | `qa_limiter_probe*.php` |
| رسم sha1 للمفاتيح → ملفات الكاش | `qa_sha1*.php`, `qa_mapfiles.php` |
| فحص جدول الكاش في DB | `qa_cache_db*.php` |
| ترميم Router الصحيح للخادم | `router.php` (مع `-t public`) |

---

## 5. ملاحظات البيئة وأفضل الممارسات

1. **سرعة الخادم**: `php -S` أحادي الخيط + File Cache؛ عند تفريغ الكاش تستغرق الصفحة الأولى ~9 ثوانٍ (إعادة بناء جميع الإعدادات). المجموعات قد تصاب بـ timeout 30s مباشرة بعد التفريغ — أعد التشغيل بعد تسخين الكاش.
2. **دقة عناوين `*.localhost`**: المتصفحات تحل `*.localhost` (RFC 6761) لكن curl/nslookup لا تحلها — اختبارات النطاقات الفرعية تتم عبر Playwright حصريًا.
3. **تشفير `google2fa_secret`**: الموديل يفرض cast من نوع `encrypted` — أي كتابة SQL مباشرة تجعل القراءة تفشل (500). اكتب عبر `Crypt::encryptString`.
4. **المعرّفات عامة (global auto-increment)**: قيم ID غير محصورة بالمستأجر في الجداول — عزل المستأجرين مُثبت عبر النطاق (TenantScope/بوليصة)، لذلك فحص FUZZ عبر معرّفات كيانات أخرى → 404.
5. **أخطاء بيئية طبيعية (تُتجاهل)**: `smtp.example.com` فاشل، `REQFAIL /api/health-check`، `/_debugbar/open` 404، `network-monitor.css` إلخ.
6. **الخادم الصحيح**: `php -S 127.0.0.1:8000 -t public C:\...\router.php` — بدون `-t public` تفشل الأصول الثابتة (JS/CSS) بـ 404.

---

## 6. الحالة النهائية للبيانات (Pristine)

| الكيان | الحالة |
|--------|--------|
| المستأجرون | 3 (demo-center، ra3y، QA Center 194003 — fixture) |
| الكورسات | 4 (1-2 لـ T1، 3-4 لـ T3) |
| الجداول | 10 |
| الحجوزات | 0 |
| الطلاب | 15 (1-10 لـ T1، 24-28 لـ T3) |
| الأيتام (schedules/sections/enrollments بلا مرجع) | 0 |
| بقايا QA (مستأجرون/كورسات/مبيعات/مستخدمون/مشرفون) | 0 |

---

## 7. توصيات مستقبلية

1. استبدال File Cache بـ Redis في الإنتاج (يُزيل بطء إعادة البناء ويجعل الـ throttle عالي الدقة).
2. رفع timeout المجموعات إلى 45-60 ثانية أو تسخين الكاش قبل `delcounters`.
3. إضافة مجموعة اختبار دائمة لـ BUG-027/028 (إنشاء طالب بعد حذف سابق → كود جديد صحيح).
4. Centralize ثوابت الـ limiter المسماة في `bootstrap/app.php` لتجنّب تعارض المفاتيح مستقبلًا.